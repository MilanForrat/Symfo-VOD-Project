{% if field.value == 1 %}
    <span class="badge text-bg-secondary p-2">En attente de paiement</span>
{% elseif field.value == 2 %}
    <span class="badge text-bg-success p-2">Paiement validé</span>
{% endif %}